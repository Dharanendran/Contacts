pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral{url 'https://maven.google.com'}
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Contacts"
include ':app'
